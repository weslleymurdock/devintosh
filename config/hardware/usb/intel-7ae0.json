{
  "schemaVersion": 1,
  "id": "intel-usb-xhci-7ae0",
  "description": "Intel USB 3.x xHCI controller Device ID 7AE0.",
  "match": {
    "usbVendorId": "8086",
    "usbDeviceIds": ["7AE0"]
  },
  "capabilities": {
    "usb": true,
    "usbController": "Intel-XHCI",
    "requiresMacOsUsbPortValidation": true
  },
  "opencore": {
    "policy": "validation-required",
    "usb": {
      "strategy": "validation-required",
      "requiredEvidence": [
        "Native macOS USB controller and port inventory",
        "Validated physical port to macOS port mapping",
        "Validated USB connector types and internal/external topology"
      ],
      "prohibitedAutomation": [
        "Do not derive a macOS USB port map from Windows PnP names or hub enumeration.",
        "Do not enable XhciPortLimit as a generic replacement for a validated port map.",
        "Do not inject USB topology changes without an explicitly validated profile."
      ]
    },
    "kernel": {
      "policy": "no-automatic-xhci-port-limit"
    }
  }
}
