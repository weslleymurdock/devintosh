# `scripts/lib/console.ps1`

Provides shared console formatting, status output, and terminal presentation used by the pipeline.

It keeps interactive output consistent without embedding hardware-specific behavior.
