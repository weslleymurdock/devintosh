# `scripts/lib/logging.ps1`

Provides structured console/file logging helpers shared by pipeline stages.

The library standardizes severity and message formatting and keeps logging independent from hardware-specific decisions.
