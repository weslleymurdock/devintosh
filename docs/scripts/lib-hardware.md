# `scripts/lib/hardware.ps1`

Contains reusable Windows hardware inspection and identity helpers.

The library reports observed identities such as CPU, GPU, platform, and PnP information. It does not decide how a device should be spoofed or configured; those decisions belong to declarative profiles.
