# `scripts/lib/progress.ps1`

Provides the shared progress-bar implementation used by pipeline stages.

The library is presentation-only: it does not make configuration decisions or inspect hardware.
